<% _.each(filters, function(filter) { %>
  <div class="field">
    <label><%= filter.key %></label>
    <input type="text" name="<%= filter.key %>" value="<%= data[filter.key] %>">
  </div>
<% }); %>
<button type="submit">Save</button>