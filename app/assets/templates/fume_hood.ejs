<div class="admin-row">
  <%= external_id %>
  <% _.each(data, function(value, key) { %>
    <% if (value) { %>
      <span class="metadata-bar"><%= value %></span>
    <% } else { %>
      <span class="metadata-bar metadata-empty">(<%= key %>)</span>
    <% } %>
  <% }); %>
</div>

